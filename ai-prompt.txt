CURRENT CONTEXT (Kurzfassung)
=============================
Ziel: Menüanzeige und fehlerfreie Tastatur-Navigation wiederherstellen und alle Fixes in die modulare `src/`-Codebasis übernehmen.

Letzter Stand:
- Modularer Build erzeugt `run.sh` (aus `src/`-Modulen).
- Fehlende Funktionen wurden ergänzt; `parse_config_vars()` und `load_task_vars()` sind mit Fehlerbehandlungs-Wrappers versehen.
- Aktuelles Blocker-Symptom: Skript beendet sich während der Startsequenz nach `detect_config_files` (Exit Code 1). `bash -x`-Trace zeigt zuletzt: `parse_config_vars`, `load_settings`, `load_state`, `detect_config_files`, dann `rm -rf /tmp/.run_cache_*` und Exit.

Wichtige Befunde (Debugging):
- Ursache (teilweise): strenge `set -euo pipefail` zusammen mit Pipelines (z.B. `grep | cut | xargs`) führte zu unerwarteten Exit-Pfaden. `set +e` allein reicht nicht, `pipefail` muss ebenfalls temporär deaktiviert oder Pipelines mit `|| true` abgesichert werden.
- `load_aliases()` wird aufgerufen; `parse_config_vars()` läuft mehrfach und enthält inzwischen `set +e +o pipefail` beim Lesen von `config_path`.

Reproduktionsbefehle (lokal)
----------------------------
1) Vollständiger Build:

```bash
./build.sh --all
``` 

2) Trace-Run zur Fehlersuche:

```bash
bash -x ./run.sh docker 2>&1 | tail -100
``` 

3) Schneller Menü-Exit-Test:

```bash
# send a 'q' to quit the menu once it appears
bash -c '(sleep 0.5; echo "q")' | ./run.sh docker
```

Worauf achten im Trace:
- Reihenfolge: `parse_config_vars` → `load_settings` → `load_state` → `detect_config_files` → `load_aliases` → `check_interactive` → `main_interactive_loop`.
- Letzte ausgeführte Befehle (Cache-Aufräumen `/tmp/.run_cache_*`).

Empfohlene nächste Schritte
--------------------------
1) Neu bauen (falls noch nicht): `./build.sh --all`.
2) Trace laufen lassen: `bash -x ./run.sh docker 2>&1 | tail -200`.
3) Falls Exit vor `load_aliases`/`check_interactive`: prüfe `detect_config_files()` und `load_aliases()` in `src/` auf unbeabsichtigte `exit`/`return` oder Pipelines ohne `|| true`.
4) Temporäre Debug-prints kurz in `src/16-main.sh` vor/nach `load_aliases`, `check_interactive`, `main_interactive_loop` setzen, neu bauen und Trace prüfen.
5) Sobald Menü erscheint: teste Tastatur-Fix in `src/13-ui.sh` (`stty raw -echo; dd bs=1 count=1`) und verifiziere, dass Pfeiltasten nicht in den Editor führen.

Kurz-Checklist für die nächste Sitzung
-------------------------------------
- `./build.sh --all`
- `bash -x ./run.sh docker 2>&1 | tail -200`
- Wenn nötig: Debug-prints in `src/16-main.sh` hinzufügen und erneut bauen
- Wenn Menü sichtbar: Tastatur testen (Pfeil hoch/runter)

Frage an dich: Soll ich jetzt automatisch Build + Test-Run ausführen und die Trace-Ausgabe hier anhängen? Wenn ja, bestätige kurz, dann führe ich es aus.

